[package]
name = "furnigen-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Wardrobe spec types, validation, and geometry (FurniGen)"

[dependencies]
thiserror = "2.0"
